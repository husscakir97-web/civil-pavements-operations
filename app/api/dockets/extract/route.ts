// All paid extraction must pass through the same billing/consent gate.
// The old text-only route must not incur provider costs merely because a key exists.
export {POST} from '@/app/api/ai-scans/route';
