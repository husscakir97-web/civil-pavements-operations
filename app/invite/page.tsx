'use client';
import {useState} from 'react';
export default function Invite(){const [message,setMessage]=useState('Sign in with the invited email address, then accept this invitation.');return <main className="mx-auto max-w-lg space-y-5 p-8"><h1 className="text-2xl font-bold">Join your organisation</h1><p>{message}</p><a className="underline" href="/login">Sign in or create an account</a><p>Accepting changes your active organisation. Any records in your previous organisation remain there.</p><button className="rounded bg-orange-600 p-3 text-white" onClick={async()=>{const token=new URL(location.href).searchParams.get('token');const r=await fetch('/api/invitations',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});const data=await r.json();if(r.ok)location.assign('/');else setMessage(data.error)}}>Accept invitation</button></main>}
